<p id="notice"><%= notice %></p>

<p>
  <strong>名前:</strong>
  <%= @event.title %>
</p>

<p>
  <strong>説明:</strong>
  <%= @event.description %>
</p>

<p>
  <strong>開始時間:</strong>
  <%= @event.start_date %>
</p>

<p>
  <strong>終了時間:</strong>
  <%= @event.end_date %>
</p>

<% if user_signed_in? && current_user.id == @event.user_id %>
  <%= link_to '予約変更', edit_event_path(@event) %> |
<% end %>
<%= link_to 'ページを戻る', events_path %>
